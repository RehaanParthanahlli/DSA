ARRAY / SEQUENCE ROADMAP (BEGINNER → ADVANCED)

STAGE 0 — CORE IDEA (NON-NEGOTIABLE) ---------------------> Read from that PUC II handbook
- What is a sequence (ordered data)
- What is an array (contiguous memory)
- Homogeneous data
- Index-based access                                  
- Why arr[i] is O(1)
- Why memory contiguity matters

STAGE 1 — BASIC OPERATIONS + COST ------------------------> ChatGPT
- Access by index → O(1)
- Traversal → O(n)
- Insert at end → O(1)* amortized
- Insert at beginning/middle → O(n)
- Delete → O(n)
- Understand shifting cost (left/right)

STAGE 2 — STATIC vs DYNAMIC ARRAYS
- Static arrays (fixed size)
- Dynamic arrays (resizable)
- Size vs Capacity
- Reallocation strategy
- Copying during resize
- Amortized analysis (must)

STAGE 3 — MEMORY MODEL
- Stack vs Heap allocation
- Contiguous layout
- Address calculation formula
- Cache friendliness
- Why arrays are faster than linked lists in practice

STAGE 4 — INDEX & BOUNDARY LOGIC
- 0-based indexing
- Off-by-one errors
- i, i+1, n-1 logic
- Loop boundaries
- Edge cases: empty, single element

STAGE 5 — ARRAY PATTERNS (MOST IMPORTANT)
- Linear scan
- Two pointers
- Sliding window
- Prefix sum
- Frequency array
- In-place modification
- Reverse / rotate patterns

STAGE 6 — MULTI-DIMENSIONAL ARRAYS
- 2D arrays memory layout
- Row-major order
- Matrix traversal patterns
- Boundary handling
- Flattening 2D to 1D

STAGE 7 — VARIANTS OF ARRAYS
- Dynamic array (vector / ArrayList)
- Circular array
- Sparse array
- Difference array
- Rolling array (space optimization)

STAGE 8 — ARRAYS vs OTHER SEQUENCES
- Array vs Linked List
- Array vs Deque
- Array vs Hash-based structures
- When NOT to use arrays

STAGE 9 — ADVANCED / SYSTEMS VIEW
- Cache locality
- False sharing (intro)
- Memory alignment
- Large array pitfalls
- Real-world usage (databases, OS, ML)

CORE TRUTHS (80/20 RULE)
- Arrays are fast because of memory layout
- Insert/delete is expensive except at end
- Dynamic arrays hide copying cost
- Index math causes most bugs
- Patterns matter more than syntax
